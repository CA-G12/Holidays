const getCountries = require('./countries');
const getCurrentLocation = require('./currentLocation');
const search = require('./search');

module.exports = { getCountries, getCurrentLocation, search };
